icon: https://apps.okteto.com/movies/icon.png

build:
  rentals:
    context: .

deploy:
  - okteto deploy -f mongodb-compose.yml -l=debug
  - helm upgrade --install rentals chart --set image=${OKTETO_BUILD_RENTALS_IMAGE}

dev:
  rentals:
    command: yarn start
    sync:
      - .:/src
